run `node ./scripts/toddle.js` to add toddle signal. then run `npm run install-peer` to install again all peers dependencies for other packages.

this is not exported because toddle signal is not a package that you can import.
