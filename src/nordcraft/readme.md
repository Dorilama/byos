run `node ./scripts/nordcraft.js` to add nordcraft signal. then run `npm run install-peer` to install again all peers dependencies for other packages.

this is not exported because nordcraft signal is not a package that you can import.
